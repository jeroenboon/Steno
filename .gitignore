node_modules/
out/
dist/
coverage/
.DS_Store
*.local

# TypeScript composite build info
*.tsbuildinfo

# Stray build/test-run logs (e.g. `npm test > test-output.txt`, `npm run lint > lint-output.txt`)
*-output.txt

# Editor
.vscode/
.idea/

# OS
Thumbs.db

# Dev-only debug log (main process; may contain content in --debug mode)
logs/

# Local-only session artefacts (never commit these)
design_todo.md
docs/handoff-*.md
docs/design/screenshots/
scripts/visual-inspect.mjs

# Local-only test audio fixtures (never commit these)
test/
*.m4a

# Local-only design-preview harness (renders the screens in a browser with a
# stubbed window.api; see the header in src/renderer/src/preview.tsx). Excluded
# from the build and from eslint / tsc / prettier so it never trips the DoD gate.
/preview.html
/vite.preview.config.mjs
src/renderer/src/preview.tsx

# Agent prompts / copilot customisation (workspace-local, not source)
.github/prompts/
